# 📊 Order Confirmation Email - System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        EMAIL SYSTEM FLOW                             │
└─────────────────────────────────────────────────────────────────────┘

1️⃣  ADMIN ACTION
    ┌──────────────┐
    │  Admin Panel │  Admin selects order, clicks
    │  OrdersTab   │  "📧 Send Confirmation Email"
    └──────┬───────┘
           │
           ↓
    ┌──────────────────────────────────────────────────┐
    │ Confirmation Dialog:                             │
    │ "Send email to customer@email.com?"              │
    │ Order ID: abc12345                               │
    │ Total: ₹1,299                                    │
    │ Items: 3 product(s)                              │
    └──────────────┬───────────────────────────────────┘
                   │ [Confirm]
                   ↓

2️⃣  API CALL
    ┌──────────────┐
    │  Frontend    │  POST request with order data:
    │  React App   │  - orderId, customerEmail
    └──────┬───────┘  - items, address, payment
           │          - totalPrice, createdAt
           │
           ↓
    Environment detection:
    • localhost → http://localhost:3001/api/send-email
    • production → https://freelit.in/api/send-email

3️⃣  BACKEND PROCESSING
    ┌──────────────┐
    │ Express      │  Receives request
    │ Server       │  Port: 3001
    └──────┬───────┘
           │
           ↓
    ┌──────────────────────────────────────────────────┐
    │ api/send-email.js                                │
    │ • Validates request                              │
    │ • Formats order date                             │
    │ • Calls emailTemplate.js                         │
    │ • Generates HTML + plain text                    │
    └──────────────┬───────────────────────────────────┘
                   │
                   ↓
    ┌──────────────────────────────────────────────────┐
    │ api/emailTemplate.js                             │
    │ • getOrderEmailTemplate() → HTML version         │
    │ • getOrderEmailText() → Plain text version       │
    │ • Uses config: colors, storeName, etc.           │
    └──────────────┬───────────────────────────────────┘
                   │
                   ↓

4️⃣  SMTP DELIVERY
    ┌──────────────┐
    │  Nodemailer  │  Creates email with:
    │  Transporter │  • From: "Freelit" <care@freelit.in>
    └──────┬───────┘  • To: customer@email.com
           │          • Subject: Order Confirmed - #abc12345
           │          • HTML + Text versions
           │
           ↓
    ┌─────────────────────────────────┐
    │  Hostinger SMTP Server          │
    │  smtp.hostinger.com:465         │
    │  • Authenticates with SMTP creds │
    │  • Checks DNS records (SPF/DKIM) │
    │  • Sends email                   │
    └──────────────┬──────────────────┘
                   │
                   ↓
    ┌──────────────────────────────────────────────────┐
    │  DNS RECORDS (Hostinger)                         │
    │  ✅ SPF: Authorizes Hostinger to send           │
    │  ✅ DKIM: Cryptographic signature                │
    │  ✅ DMARC: Email policy rules                    │
    └──────────────┬───────────────────────────────────┘
                   │
                   ↓

5️⃣  EMAIL DELIVERY
    ┌──────────────────────────┐
    │  Customer's Email Server │
    │  (Gmail/Yahoo/etc)       │
    │  • Checks DNS records    │
    │  • Validates signature   │
    │  • Spam score check      │
    └──────────────┬───────────┘
                   │
                   ↓
           ┌───────────────┐
           │   📬 INBOX    │  ← Goal!
           └───────────────┘
                  or
           ┌───────────────┐
           │   📁 SPAM     │  ← Without DNS records
           └───────────────┘

6️⃣  STATUS UPDATE
    ┌──────────────┐
    │  Frontend    │  Email sent successfully!
    │  Response    │  • Shows success toast
    └──────┬───────┘  • Updates order status to "confirmed"
           │          • Refreshes orders list
           ↓
    ┌──────────────┐
    │  Supabase    │  Update orders table:
    │  Database    │  SET status = 'confirmed'
    └──────────────┘  WHERE id = orderId


═══════════════════════════════════════════════════════════════════

📂 FILE STRUCTURE
═══════════════════════════════════════════════════════════════════

FRONTEND (React)
└── src/components/admin/
    └── OrdersTab.tsx
        • handleSendConfirmationEmail()
        • Detects localhost vs production
        • Calls API endpoint

BACKEND (Node.js)
├── server.js
│   • Express server (port 3001)
│   • Routes: /api/send-email, /api/test-email, /api/health
│
├── api/
│   ├── send-email.js
│   │   • Nodemailer configuration
│   │   • Email sending logic
│   │   • CORS handling
│   │
│   └── emailTemplate.js  ⭐ CUSTOMIZE THIS!
│       • emailConfig (colors, store info)
│       • getOrderEmailTemplate() - HTML
│       • getOrderEmailText() - Plain text
│
└── .env.email
    • SMTP_USER=care@freelit.in
    • SMTP_PASS=Sathwikreddy77wk$
    • PORT=3001


═══════════════════════════════════════════════════════════════════

🌐 DEPLOYMENT ARCHITECTURE
═══════════════════════════════════════════════════════════════════

OPTION A: Hostinger (Same host for everything)
┌─────────────────────────────────────────────────────────────────┐
│                         freelit.in                               │
│                      (Hostinger Hosting)                         │
│                                                                  │
│  ┌────────────────┐              ┌──────────────────┐          │
│  │  React App     │              │  Node.js Server  │          │
│  │  (Frontend)    │──────calls──→│  port 3001       │          │
│  │  /public_html/ │              │  server.js       │          │
│  └────────────────┘              │  api/*           │          │
│                                   └──────┬───────────┘          │
│                                          │                       │
│                                          ↓                       │
│                                   ┌──────────────┐              │
│                                   │ Hostinger    │              │
│                                   │ SMTP Server  │              │
│                                   └──────────────┘              │
└─────────────────────────────────────────────────────────────────┘


OPTION B: Hybrid (Backend on Railway)
┌──────────────────────┐         ┌─────────────────────────────┐
│   freelit.in         │         │   Railway.app               │
│  (Hostinger)         │         │   (Backend Server)          │
│                      │         │                             │
│  ┌────────────────┐ │         │  ┌──────────────────┐       │
│  │  React App     │─┼─calls──→│  │  Node.js Server  │       │
│  │  (Frontend)    │ │         │  │  server.js       │       │
│  └────────────────┘ │         │  │  api/*           │       │
│                      │         │  └──────┬───────────┘       │
└──────────────────────┘         │         │                   │
                                 │         ↓                   │
                                 │  ┌──────────────┐           │
                                 │  │ Hostinger    │◄──uses    │
                                 │  │ SMTP         │           │
                                 │  └──────────────┘           │
                                 └─────────────────────────────┘


═══════════════════════════════════════════════════════════════════

🔧 CONFIGURATION SUMMARY
═══════════════════════════════════════════════════════════════════

✅ VERIFIED SETTINGS:

Frontend URL:
  Development: http://localhost:3001/api/send-email
  Production:  https://freelit.in/api/send-email

SMTP Configuration:
  Host:     smtp.hostinger.com
  Port:     465 (SSL)
  User:     care@freelit.in
  Password: Sathwikreddy77wk$

Email Template:
  Store:    Freelit
  Email:    care@freelit.in
  Website:  https://freelit.in
  Colors:   Brown (#5e4338) + Mint (#b5edce)

Server Endpoints:
  POST /api/send-email  - Send order confirmation
  POST /api/test-email  - Test with mail-tester.com
  GET  /api/health      - Health check


═══════════════════════════════════════════════════════════════════

⚠️  CRITICAL: DNS RECORDS REQUIRED
═══════════════════════════════════════════════════════════════════

Without these, emails go to spam!

Add in Hostinger DNS Manager:

┌─────────────────────────────────────────────────────────────────┐
│ SPF Record                                                       │
│ Type: TXT                                                        │
│ Name: @                                                          │
│ Value: v=spf1 include:_spf.hosting.hostinger.com ~all          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ DMARC Record                                                     │
│ Type: TXT                                                        │
│ Name: _dmarc                                                     │
│ Value: v=DMARC1; p=none; rua=mailto:care@freelit.in           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ DKIM (Enable in Hostinger Email Settings)                       │
│ Hostinger → Email → Advanced → DKIM → Enable                   │
│ Then copy the generated record to DNS                           │
└─────────────────────────────────────────────────────────────────┘

Wait 1-24 hours for DNS propagation!


═══════════════════════════════════════════════════════════════════

📊 TESTING FLOW
═══════════════════════════════════════════════════════════════════

1. Deploy server
   ↓
2. Test health endpoint: curl https://freelit.in/api/health
   ↓
3. Deploy frontend: npm run build → upload dist/
   ↓
4. Create test order with mail-tester email
   ↓
5. Send confirmation from admin panel
   ↓
6. Check mail-tester.com for score
   ↓
7. If score < 8: Add DNS records
   ↓
8. Wait 24 hours
   ↓
9. Re-test
   ↓
10. Score 8-10/10? ✅ Production ready!


═══════════════════════════════════════════════════════════════════
```
