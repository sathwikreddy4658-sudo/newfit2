# Development overrides — Vite proxy routes /firebase-api/* to Firebase Cloud Functions
# so the browser makes same-origin requests and CORS is never an issue in local dev.
VITE_PHONEPE_FUNCTIONS_URL=/firebase-api
